{
  "name": "@resume/synthetic-ats",
  "private": true,
  "type": "module",
  "exports": {
    "./server": "./src/server.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json",
    "build": "tsc --noEmit -p ../../tsconfig.json"
  }
}
