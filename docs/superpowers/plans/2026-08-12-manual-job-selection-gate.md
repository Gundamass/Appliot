# 手动选岗填写门禁实施计划

**目标：** 新建投递后只等待用户完成登录和选岗；仅在进入简历上传页或包含必填简历字段的申请表单后启动自动填写。

## 实施步骤

1. 在投递服务中增加可填写页面判定，过滤只有搜索/筛选字段的职位列表页。
2. 等待期间忽略不会冲突的用户点击；页面稳定后重新观察并在命中表单时继续现有填写流程。
3. 更新任务状态文案，明确显示“等待进入简历填写页面”。
4. 增加 4399 风格职位列表到申请表单的回归测试，并运行 API、Web、构建和类型检查。
